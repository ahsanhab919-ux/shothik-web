# Vercel Parity Remediation - 2026-07-21

## Scope

- Assigned task: continue the Writing Studio environment-parity rollout across
  local development and Vercel.
- Focused gap: `API_KEY_SALT` was documented as required but absent from local
  secure env files.
- Hard constraint: preserve existing preview and production secrets unless an
  explicit rotation window is approved.

## Requirements Baseline

- `API_KEY_SALT` is a server-only application secret generated by the operator.
- `STRIPE_SECRET_KEY` remains an external provider credential and was not
  provisioned from this workspace.
- `PREVIEW_ACCESS_ALLOWED_EMAILS` and
  `PREVIEW_ACCESS_REQUIRED_SCOPES` remain environment-policy inputs owned by the
  preview access contract.

## Milestones

1. Baseline and requirements review
   - Confirm env ownership and production-rotation risk.
2. Safe implementation
   - Generate a new local/dev `API_KEY_SALT`.
   - Sync the same value into Vercel `development`.
3. Validation
   - Reconfirm local env presence.
   - Reconfirm Vercel development env registration.
   - Run targeted unit coverage for the Vercel env audit helper.
4. Documentation and handoff
   - Extend environment documentation with generation and rotation guidance.
   - Record remaining blockers for final review.

## Implemented Changes

- Added `API_KEY_SALT` to local ignored `/.env.local`.
- Preserved existing Vercel `preview` and `production` salts to avoid
  unplanned API key invalidation.
- Added preview access policy values to local ignored `/.env.local` using the
  smoke test account as the allowlisted email and `preview:access` as the
  required scope.
- Synced the preview access policy values into Vercel `preview`.
- Documented generation and rotation guidance in `docs/ENVIRONMENT_VARIABLES.md`.

## Validation Plan

- Unit test: `pnpm exec vitest run test/vercel-env-audit.test.ts`
- Local env check: verify `API_KEY_SALT` is present after sourcing `.env.local`.
- Deployment check: verify `API_KEY_SALT` exists in Vercel `development`.

## Remaining Blockers

- `STRIPE_SECRET_KEY` still requires provider-backed provisioning for complete
  parity in payment-enabled environments.
- Protected preview access still depends on the authenticated smoke account
  actually carrying the `preview:access` scope at runtime.
- Any future `API_KEY_SALT` rotation for `preview` or `production` requires a
  deliberate migration decision.
